
<div class="page-content page-thin">
<%= render partial: 'shared/page_title_caption', object: "创建#{@training_class.name}的#{@homework.subject_type}作业 " %>

<%= render 'form' %>

<%= link_to '返回到班级作业 列表', training_class_homeworks_path(@training_class),{class: 'btn btn-primary'} %>
</div>